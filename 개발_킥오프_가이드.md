# Suyo 백엔드 개발 킥오프 가이드

최종 갱신 2026-08-13. Claude Code + IntelliJ + PostgreSQL로 백엔드를 개발할 때 보는 문서.

> **함께 볼 문서**
> - `레이어_점수산출_설계서.md` — 점수 계산 로직 전체 (백분위 방식, 등급 경계, 지표별 배점). **Step 6 스코어링 구현 전에 반드시 읽을 것.**
> - `db-init/scoring_reference.py` — 위 설계서의 로직을 Python으로 실제 구현·검증한 스크립트 (2026-08-13 v3: L2 40점·총점 100점 배점으로 갱신됨). Java로 옮길 때 이 계산 순서를 그대로 따르면 됨.
> - 노션 `API 명세서` — 요청/응답 JSON 스펙
> - 노션 `ERD 설계서` — 테이블 15개 상세 (2026-08-13: payment_credits 추가)

---

## ⚠️ 지금 바로 처리해야 할 미결 6건

이어서 작업할 때 **이 여섯 개부터** 확인할 것.

### 1. `DiagnosisScorer.java`·`RiskGrader.java`가 아직 v2 배점으로 남아있다 (2026-08-13 재확인 · **최우선**)

L1~L3 스코어링 로직 자체는 이미 구현돼 있다(`scoring/DiagnosisScorer.java`, `RiskGrader.java`, `PercentileRanker.java`, `ScoringCache.java`) — 처음부터 짤 필요는 없다. 다만 6번 항목의 v3 재설계(L2 28→40점)가 코드에는 반영되지 않고 문서에만 반영된 상태다. 그대로 두면 `AnalysisService`가 API 응답에 `CUSTOMER maxScore: 40`이라고 내려주면서 실제 계산되는 점수는 28점을 넘지 못하는 모순이 생긴다.

**고쳐야 할 지점 (파일:줄):**
- `DiagnosisScorer.java` L30~33: `MAX_GENDER=5→7`, `MAX_AGE=5→7`, `MAX_DEMAND=8→11`, `MAX_WEEKEND=10→15` (L20 주석의 "v2"도 "v3"로)
- `RiskGrader.java` L13~14: `L2_LOW_CUT=13.4→18.9`, `L2_HIGH_CUT=17.6→24.9`
- `RiskGrader.java` L17~18: `TOTAL_LOW_CUT=41.9→48.0`, `TOTAL_HIGH_CUT=50.3→57.9` (현재 `AnalysisService`에서 총점 등급을 실제로 매기는 곳은 없지만, 향후 총점 리스크 표시에 쓰일 수 있으니 상수 자체는 맞춰둘 것)
- `ScoringCache.java` L27 주석의 "v2"도 "v3"로

값의 근거는 `레이어_점수산출_설계서.md` 1·2번 섹션과 `db-init/scoring_reference.py`(이미 v3로 갱신됨) 참고. L1(`MAX_SIZE/CAGR/MOMENTUM`)과 L3(`MAX_DENSITY/SURVIVAL`), L1·L3 등급컷은 이미 맞게 들어가 있어 손댈 필요 없다.

**참고 — 이미 완료된 것으로 착각하기 쉬운 부분**: `PopulationBySgg` 엔티티·리포지토리는 이미 만들어져 있고(`domain/PopulationBySgg.java`, `repository/PopulationBySggRepository.java`), `DiagnosisResult`의 점수 필드도 이미 `BigDecimal(precision=4, scale=1)`로 돼 있다. 예전 버전 가이드에 "없다"고 적혀 있었는데 착오였다 — 지금은 둘 다 정상이니 다시 만들 필요 없다.

### 2. 업종 매칭이 96개 제한 없이 247개 전체와 매칭된다

`IndustryMatcher.java`는 텍스트 매칭 1단계(`industryCodes` 전체 순회)에서 **매핑 여부와 상관없이 247개 소분류 전체**를 대상으로 EXACT 매칭을 시도한다. 그런데 아래 3번 항목에서 이미 "업종 검색을 매핑된 96개로 제한"하기로 결정했고, 그 이유가 "매핑 안 된 업종을 고르면 L1·L2가 통째로 빈다"였다. 지금 코드는 그 결정 이전 상태 그대로다 — 예를 들어 사용자가 매핑 안 된 151개 중 하나(예: "법무사")를 정확히 입력하면 `MatchAccuracy.EXACT`로 매칭되고 `DiagnosisScorer`에서 `metrics == null` 분기를 타서 `dataCoverage=COMPETITION_ONLY`가 정상 플로우에서 그대로 나온다. 3번 항목의 결정이 코드에는 아직 반영 안 된 상태이니, `GET /api/industries?q=` 엔드포인트 구현과 함께 `IndustryMatcher`의 1단계 매칭 대상도 96개(`industry_code_mapping`)로 제한하도록 고칠 것.

### 3. 업종 검색을 96개 매핑 업종으로 제한하기로 결정 (2026-08-12 신규)

기존 설계는 자유 텍스트 아이템명을 247개 소분류 전체와 매칭했는데, 그중 151개(전국사업체수 기준 28.3%, 주로 컨설팅·법무·설계 등 B2B)는 서울 매출데이터와 매핑이 안 돼 있어 L1·L2가 통째로 빈다(`dataCoverage=COMPETITION_ONLY`). 절반 넘는 입력에서 반쪽짜리 결과를 보여주는 게 서비스 신뢰도를 깎는다고 판단해서, **업종 선택 자체를 매핑된 96개로 제한**하기로 결정함.

- 96개가 전국사업체수 기준 71.7%를 커버하고, 예비창업자가 실제로 찾을 법한 외식(76.7%)·소매(53.1%)·부동산(100%) 업종 상위권은 대부분 포함됨. 안 잡히는 151개는 원천 데이터(서울시 상권분석서비스)가 애초에 63개 업종만 분류해서 생기는 구조적 한계라 매핑을 더 늘릴 여지가 없음.
- 구현 필요: `GET /api/industries?q=` 자동완성 엔드포인트 신규 (96개만 반환, 대분류로 그룹핑). `POST /api/analyses`에 `industryCode` 필드 추가해서 이 목록에서 선택하도록 변경. 자유 텍스트 기반 유사 업종 매칭은 96개 안에서만 수행.
- 결과: `dataCoverage=COMPETITION_ONLY`는 정상 플로우에서 나오지 않음(방어 로직은 유지). `matchAccuracy=APPROXIMATE`도 96개 안에서만 발생.
- 상세 스펙: 노션 API 명세서 "0. 지원 업종 조회" 섹션.

### 4. `DiagnosisResult`·`Questionnaire`에 AI 생성 콘텐츠용 컬럼이 없다 (2026-08-12 신규)

종합 결과 화면에 "왜 이 점수가 나왔는지" AI 종합진단 문구를 추가하고, 질문지 화면의 "유도질문 검사 통과" 카드를 실제 생성된 질문 기반 동적 문구로 바꾸기로 했다(레이어별 AI 코멘트가 아니라 총점 단계에 하나로 모으는 방식 — 화면이 덜 산만함). 둘 다 **LLM이 새 숫자·근거를 만들어내는 게 아니라 이미 계산·생성된 내용을 자연어로 설명만 하는 것**이라 "L1/L2/L3 숫자는 LLM으로 만들지 않는다" 원칙과 충돌하지 않는다.

- `diagnosis_results`에 `ai_summary TEXT` (nullable) 추가 필요
- `questionnaires`에 `leading_question_check_passed BOOLEAN`, `leading_question_check_summary TEXT` 추가 필요
- 상세 스펙: `레이어_점수산출_설계서.md` 9·10장, 노션 API 명세서 4·6번 섹션, 노션 ERD 설계서

### 5. 유료화(freemium) 모델 확정 — 신규 (2026-08-13)

디자이너가 Figma에 결제 플로우를 이미 만들어뒀는데(01·03·06·07번 화면) 지금까지 백엔드 스펙 어디에도 결제가 없었다. 이번에 모델을 확정했다.

**요금제 — 구독 없음, 건별 결제 2종만**

| 플랜 | 가격 | 내용 |
|---|---|---|
| 무료 | 0원 | 종합 점수 + 판정만 (`totalScore`, `verdict`, `dataCoverage`) |
| 1건 | 9,900원 | 결제한 진단 1건의 레이어 상세·근거·AI 종합진단·질문지 생성·리포트 열람 전부 해제 |
| 3건 팩 | 24,900원 (건당 8,300원) | 결제 시점 진단 1건 즉시 해제 + **크레딧 2건**을 세션에 지급, 6개월 내 다른 진단에 자동 적용 |

~~월 무제한 구독~~은 검토했다가 **뺐다** (2026-08-13). 정기결제·해지 로직까지 만들 여유가 없고, 건별 2종만으로도 데모에 충분하다는 판단.

**인증이 없는데 "내 아이템"·크레딧을 어떻게 추적하나**

로그인은 여전히 없다. 대신 **익명 세션**을 쓴다 — 프론트가 최초 방문 시 UUID를 만들어 `localStorage`에 저장하고, 이후 모든 요청에 `X-Session-Id` 헤더로 실어 보낸다. 서버는 이 값으로 "내 아이템" 목록과 크레딧 잔액을 구분한다. 회원가입은 아니고 브라우저 단위 식별자다 — 캐시 지우면 이력이 끊기는 것도 감안한 해커톤 범위의 타협.

**DB 변경 필요**

- `analysis_requests`에 `session_id VARCHAR(64)`, `payment_status VARCHAR(20)`(`FREE`/`PAID`) 컬럼 추가
- 신규 테이블 `payment_credits`: `id, session_id, plan(SINGLE/PACK3), amount, credits_total, credits_remaining, expires_at, created_at`
- 새 진단 생성 시: 세션에 `credits_remaining > 0`인 유효 레코드가 있으면 **자동으로 크레딧을 1 차감하고 즉시 `payment_status=PAID`**로 생성한다 (3건팩 산 사용자가 다음 진단부터 결제 없이 보는 경험)

**API 변경 필요**

- `GET /api/analyses/{id}/diagnosis` 응답에 `accessLevel: "FREE"|"PAID"` 추가. `FREE`면 `layers[].factors`, `layers[].summary`, `aiSummary`를 `null`로 내린다 — 프론트는 이 상태를 블라인드(블러) UI로 렌더링한다. 총점·판정·`riskLevel`은 FREE에서도 그대로 내려줌 (점수 자체는 항상 보여주고 "왜"만 가린다는 게 06번 화면의 의도)
- 신규 `POST /api/analyses/{id}/payments` — `{ plan: "SINGLE"|"PACK3", paymentMethod: "KAKAOPAY"|"TOSSPAY"|"CARD" }`. **실제 PG(카카오페이 등) 연동은 하지 않는다 — 목업으로 즉시 성공 처리.** 해커톤 범위 명시. 응답에 `unlocked: true`, `plan`, `remainingCredits`, `expiresAt`
- 신규 `GET /api/credits` — 세션의 잔여 크레딧·만료일 조회 (홈 화면 "내 아이템"에 노출용)
- `POST /api/analyses/{id}/questionnaires`, 리포트 열람 관련 엔드포인트도 `payment_status=PAID` 아니면 `PAYMENT_REQUIRED`(402) 에러 — 07번 결제 화면 주문요약에 "설문지 자동 생성 ✓ · 리포트 화면 열람 ✓"이 결제 포함 항목으로 명시돼 있었기 때문
- 에러코드 `PAYMENT_REQUIRED` 402 추가
- 상세 스펙: 노션 API 명세서 신규 섹션, 노션 ERD 설계서 `payment_credits` 테이블, 노션 "무영" 페이지 요금제 섹션

### 6. L2 40점 전부 데이터화 · 총점 100점 만점으로 재설계 — 신규 (2026-08-13)

Figma를 다시 보니 화면 대부분(03·06·08·09·11)이 총점을 "X / 100"으로 표기하고 있었다. 기존 설계(L2 데이터 28점 + 정성 검증 12점, 총점 88점 만점)와 안 맞는 걸 확인하고 논의한 결과, **정성 검증 12점 자체를 없애고 L2의 기존 4개 데이터 지표 배점을 28→40으로 비례 확대**하기로 했다. 근거 백분위 순위는 그대로이고 배점만 커진 것이라 재계산 부담이 없다.

| 지표 | 기존(v2) 배점 | **신규(v3) 배점** |
|---|---|---|
| 성별 분산 | 5 | **7** |
| 연령 분산 | 5 | **7** |
| 실수요 증가율 | 8 | **11** |
| 소비패턴 안정성 | 10 | **15** |
| 정성 검증 | 12 | **삭제** |
| **L2 합계** | 28(진짜 최대) / 40(명목) | **40 (진짜 최대)** |
| **총점 합계** | 88(진짜 최대) / 100(명목) | **100 (진짜 최대)** |

**영향:**
- `db-init/scoring_reference.py`가 이미 v3 배점으로 갱신되어 있다 (`MAX` 딕셔너리, `l2`/`total` 만점 표기). Step 6 구현 시 이 파일 기준으로 그대로 옮길 것.
- `verification_results`/`customer_score_history` 관련 "검증 후 점수 갱신·프로젝션" 로직은 전부 폐기한다 — 아래 "⛔ 서비스 범위 재확정" 절도 이에 맞춰 갱신됨.
- 질문지의 역할이 "점수를 채우는 것"에서 "이미 나온 점수의 이유를 정성적으로 설명하는 것"으로 바뀐다. 화면 문구("미충족 니즈 10점은 설문으로만 메울 수 있습니다" 류)와 API 응답에 점수 상승을 암시하는 필드(`projectedMax` 등)를 넣지 않는다.
- 상세: `레이어_점수산출_설계서.md` 1·3·4·12·13번 섹션.

---

## ⛔ 서비스 범위 재확정: 질문지 생성까지 (2026-08-12 신규, 2026-08-13 갱신)

**"검증 결과를 다시 입력받아 점수를 갱신해주는 기능"은 만들지 않기로 했다.** 사용자가 질문지를 들고 나가서 인터뷰·설문을 한 다음 그 결과를 우리 서비스에 다시 입력하는 화면·API까지 만들려면 별도의 입력 폼, 검증, 이력 관리가 통째로 필요한데 — 이건 우리 서비스가 아니라 **질문지를 만들어주는 데서 끝**이라는 게 이번 결정이다.

- **제외**: `POST /api/analyses/{id}/verification-results`(검증 결과 입력), `GET /api/analyses/{id}/customer-history`(고객 점수 이력) — 이번 개발 범위에서 만들지 않는다. 아래 Step 10은 그대로 두되 "제외"로 표시해뒀다
- **(2026-08-13 갱신) "검증 후 점수 상승" 프로젝션 개념 자체를 폐기했다.** v3부터 L2는 100% 데이터로 40점 전부 나오므로 "검증 전 X → 검증 후 예상 최대 Y" 같은 문구·필드(`projectedMax` 등)는 API·화면 어디에도 넣지 않는다. `verification_results`/`customer_score_history` 테이블·엔티티는 이미 만들어져 있어도 그대로 두되(마이그레이션 되돌릴 필요는 없음) 관련 API·프로젝션 계산 로직은 만들지 않는다.
- 질문지는 이제 순수하게 "이미 확정된 점수의 이유를 정성적으로 설명·보강"하는 용도다 — 응답을 안 받아도 점수·등급·판정은 진단 시점에 이미 완결. 상세는 `레이어_점수산출_설계서.md` 3번 섹션.
- 서비스 플로우는 "①품목 입력 → ②진단 → ③점수/판정 → ④질문지 생성"에서 끝난다. "⑤사용자가 발송 → ⑥응답 반영"은 서비스 밖(사용자가 직접 하는 일)이고, 우리 쪽에 다시 들어오는 루프가 없다

---

**타겟 재확정**
- **서비스 지역: 서울특별시 25개 자치구로 한정.** 전국 데이터(소상공인365 등)도 조사했지만 정식 원천 데이터를 제공하지 않아 제외. 서울시 상권분석서비스만 유일하게 검증된 원천 데이터.
- **타겟 사용자: 서울 예비창업자·초기 스타트업 (메인 타겟 유지).** 투자자 타겟으로 전환하는 것도 검토했으나, 경쟁사 조사 결과 투자자 대상 AI 투자분석 서비스(QVP 등)가 이미 존재해 차별화가 어렵고, 무엇보다 우리 핵심 기능인 "질문지 자동생성 → 사용자가 직접 검증 → 점수 갱신" 루프는 창업자가 자기 손으로 고객을 인터뷰하는 워크플로우라 투자자에게는 잘 안 맞는다는 결론. 예비창업자 타겟을 유지하고, **투자자용 요약 리포트는 향후 확장 경로**로 노션 "무영" 페이지 7번 섹션에 명시해둠. 개발 범위·API·Step 순서는 전부 기존대로(예비창업자 기준) 진행하면 됨.

---

## 0. 데이터 최종 재검증 결과 (2026-08-11, ALL PASS)

`db-init/data/`에 CSV **6개**. 아래 항목 전부 재검증 통과.

| 파일 | 행수 | 쓰는 곳 | 검증 |
|---|---|---|---|
| industry_codes.csv | 247 | 공통 (업종 마스터) | 통과 |
| store_counts_by_sgg.csv | 6,004 | L3 | 서울 사업체 합계 **554,092** 원본 일치 |
| seoul_sales_quarterly.csv | 1,323 | L1·L2 | 63업종 × 21분기(20211~20261) |
| industry_code_mapping.csv | 96 | 공통 (코드 연결) | 소분류코드 유니크 |
| industry_survival_rates.csv | 11 | L3 | 대분류 10개 전부 매핑 |
| **population_by_sgg.csv** | **525** | **L3 (점포당 유동인구)** | 상주인구 합계 **9,360,421** = 서울시 실제 인구 |

FK 무결성 5종 전부 통과:
- `store_counts_by_sgg.소분류코드` → `industry_codes` 전부 존재
- `industry_code_mapping.상가소분류코드` → `industry_codes` 전부 존재, 유니크
- `industry_code_mapping.매출업종코드` → `seoul_sales_quarterly` 전부 존재
- `industry_survival_rates.대분류코드` → `industry_codes` 전부 존재
- `population_by_sgg` ↔ `store_counts_by_sgg`: 자치구 코드·이름 완전 일치, 분기 21개 일치

### 반드시 알아야 할 데이터 한계

| 한계 | 내용 |
|---|---|
| **업종 매핑 커버리지** | 247개 소분류 중 **96개만** 서울 매출데이터와 매핑됨. 나머지는 L1·L2가 `NULL` — 버그 아니라 정상 |
| **자치구별 매출 없음** | 매출은 **서울시 전체 × 업종** 단위. "중랑구 카페 매출"은 구할 수 없음 → 자치구별 침투율 계산 불가 |
| **상주인구 갱신 주기** | **연 1회.** 4분기 값이 다음 해 1~3분기에 반복 → 성장률 계산 금지 |
| **유동인구 중복** | 분기 누적치, 같은 사람이 여러 동에서 잡힘. 자치구 간 **상대 비교**로만 사용 (일평균은 ÷90) |
| **L3 표본 부족** | 6,004개 조합 중 **23.2%가 점포수 10개 미만** → `LOW_SAMPLE` 처리 필수 |

파일 위치: `db-init/schema.sql`(테이블 14개 — `payment_credits`는 아직 미반영, Step 9에서 마이그레이션 추가 필요), `db-init/load_data.sql`(적재 6단계), `db-init/data/*.csv`

---

## 1. 로컬 개발 착수 전 준비 (Step 0)

브랜치 안 나누고 바로 해도 되는, 환경 세팅 단계.

1. **PostgreSQL에 `suyo` 데이터베이스 생성**
   ```
   createdb suyo
   psql -d suyo -f db-init/schema.sql
   cd db-init/data && psql -d suyo -f ../load_data.sql
   ```
   `load_data.sql` 마지막에 있는 검증 쿼리로 행수·합계가 위 표와 일치하는지 확인할 것.

2. **`.env` 값 확인** — `DB_URL`, `DB_USERNAME`, `DB_PASSWORD`가 로컬 PostgreSQL 계정과 일치하는지. (`.env`는 이미 `.gitignore`에 포함되어 있어 안전함)

3. **누락된 의존성 추가** — `build.gradle`에 `spring-boot-starter-validation`이 빠져 있다. Request DTO에 `@NotBlank`, `@Size` 같은 검증 애노테이션을 쓸 거라면 지금 추가해야 한다.
   ```gradle
   implementation 'org.springframework.boot:spring-boot-starter-validation'
   ```

여기까지는 `main`에서 바로 커밋해도 된다 (아직 기능 코드가 아니라 세팅이라 브랜치 나눌 실익이 적음).

---

## 2. Git 브랜치 전략

혼자(또는 소수) 백엔드를 맡는 해커톤 프로젝트에는 `develop` 브랜치까지 만드는 풀 Git-flow는 과하다. 아래처럼 단순화한다.

- **`main`**: 항상 빌드되고 데모 가능한 상태만 유지. 여기 직접 커밋하지 않는다 (세팅류 제외).
- **기능 브랜치**: `<타입>/<기능-설명>` 형식, 기능 단위로 하나씩 판다. 완료되면 `main`에 머지하고 브랜치는 삭제.

이번 프로젝트 기준 브랜치 예시:

```
feat/db-entities          공공데이터 + 서비스 엔티티, 리포지토리
feat/analysis-request     분석 요청 생성 API
feat/diagnosis-scoring    3레이어 점수 계산 로직 + 진단 결과 조회 API
feat/evidence-hypothesis  근거·미검증가설 조회 API
feat/questionnaire        질문지 생성/조회 API (LLM 연동)
feat/payments             결제·크레딧 API
feat/misc-endpoints       목록/상태 조회 API
feat/error-handling       공통 응답 포맷 · 예외 처리
```

머지 방식: 혼자 개발이라 굳이 GitHub PR 안 거쳐도 되지만, 커밋 히스토리를 깨끗하게 남기고 싶으면 `git merge --no-ff`로 머지해서 브랜치 단위가 로그에 남게 하는 걸 추천한다.

```
git checkout main
git pull
git checkout -b feat/db-entities
# 작업...
git add .
git commit -m "feat: 공공데이터 참조 엔티티 및 리포지토리 추가"
git checkout main
git merge --no-ff feat/db-entities
git branch -d feat/db-entities
```

---

## 3. 커밋 컨벤션 (Conventional Commits)

기존 로그를 보니 이미 `feat:`, `chore:` 를 쓰고 있어서 (`feat: 스프링부트 프로젝트 초기 설정`, `chore: build.gradle 수정`) 그대로 이어가면 된다. 전체 타입은 이렇게 구분한다.

| 타입 | 언제 쓰나 | 이번 프로젝트 예시 |
|---|---|---|
| **feat** | 새로운 기능(API, 로직) 추가 | `feat: 분석 요청 생성 API 구현` |
| **fix** | 버그 수정 | `fix: 업종 매핑 조인 시 NULL 처리 안 돼서 500 나던 것 수정` |
| **chore** | 기능과 무관한 설정·빌드·의존성·잡무 | `chore: build.gradle에 validation 의존성 추가` |
| **docs** | 문서만 변경 (README 등, 코드 무관) | `docs: README에 로컬 실행 방법 추가` |
| **refactor** | 동작은 그대로, 코드 구조만 개선 | `refactor: 점수 계산 로직 DiagnosisScorer로 분리` |
| **test** | 테스트 코드 추가/수정 | `test: DiagnosisService 단위테스트 추가` |
| **style** | 포맷팅 등 의미 없는 변경 (세미콜론, 들여쓰기) | `style: 인덴트 정리` |

판단 기준이 헷갈리는 경우:
- **feat vs chore**: 사용자(프론트/API 호출자)가 체감할 수 있는 변화면 `feat`, 개발 환경/빌드/설정만 바뀐 거면 `chore`. 예를 들어 `application.yml`에 새 프로파일 추가는 `chore`, 새 엔드포인트 추가는 `feat`.
- **feat vs fix**: 원래 없던 걸 새로 만들면 `feat`, 원래 있었는데 잘못 동작하던 걸 고치면 `fix`.
- **테스트를 기능 커밋에 포함할지**: 해커톤처럼 빠르게 갈 때는 기능+테스트를 한 커밋(`feat`)에 같이 넣어도 무방하다. 나중에 테스트만 보강하면 그때 `test:`로 별도 커밋.

형식: `<타입>: <설명>` (제목은 명령형, 50자 내외). 본문이 필요하면 한 줄 띄우고 상세 설명 추가.

---

## 4. 단계별 개발 순서

현재 상태 (2026-08-13 재확인): Spring Boot 초기 설정 이후로 상당히 진행돼 있다 — 공공데이터·서비스 엔티티/리포지토리 전부, 공통 응답 포맷·예외 처리, `POST /api/analyses`·`GET /{id}/diagnosis`·`GET /{id}/evidence` 3개 API, 그리고 3레이어 스코어링 로직(`scoring/` 패키지: `DiagnosisScorer`, `PercentileRanker`, `RiskGrader`, `ScoringCache`)까지 이미 구현돼 있다. Step 2·3·5·6·7이 이미 상당 부분 완료 상태라는 뜻 — 아래 Step별 안내를 "0부터 만들 것"이 아니라 "이미 있는 걸 검토·수정할 것"으로 읽을 것. 남은 건 위 미결 1·2번(v2→v3 배점 반영, 96개 제한 반영)과 Step 8 이후(질문지·결제)다. `main` 브랜치 하나뿐.

### Step 1 — DB 세팅 (브랜치 없이 `main`에서)
- 위 "1. 로컬 개발 착수 전 준비" 그대로 수행
- 커밋: `chore: DB 스키마 및 공공데이터 시드 스크립트 추가`

### Step 2 — 공공데이터 참조 엔티티 (`feat/db-entities`) ✅ 완료 (2026-08-13: `PopulationBySgg`도 포함 확인됨)
- `IndustryCode`, `StoreCountBySgg`, `SeoulSalesQuarterly`, `IndustryCodeMapping`, `IndustrySurvivalRate`, **`PopulationBySgg`** 6개 전부 엔티티·리포지토리 — **완료됨** (예전엔 `PopulationBySgg`가 없다고 적혀 있었는데 착오였다, 이미 있음)
- 읽기 전용이므로 Repository는 조회 메서드 위주로 (save 안 씀)
- 커밋: `feat: 자치구별 인구 엔티티 및 리포지토리 추가`

### Step 3 — 서비스 운영 엔티티 (`feat/service-entities`) ✅ 완료 (2026-08-13: 점수 필드 타입도 이미 BigDecimal 확인됨)
- `AnalysisRequest`, `DiagnosisResult`, `LayerEvidence`, `UnverifiedHypothesis`, `Questionnaire`, `QuestionnaireItem`, `VerificationResult`, `CustomerScoreHistory` 8개 — **완료됨**, 점수 필드도 이미 `BigDecimal(4,1)`로 돼 있어 추가 작업 불필요
- 로컬은 `ddl-auto: update`라 엔티티 작성 후 실행해서 `schema.sql`과 스키마가 어긋나지 않는지 확인
- 참고: `Questionnaire`에는 아직 `leading_question_check_passed`/`leading_question_check_summary`, `DiagnosisResult`에는 아직 `ai_summary` 컬럼이 없음 — 위 미결 4번에서 추가할 것. `analysis_requests`의 `session_id`/`payment_status`, 신규 `payment_credits` 테이블도 아직 없음 — 미결 5번에서 추가할 것

### Step 4 — 공통 응답 포맷 · 예외 처리 (`feat/error-handling`)
- API 명세서의 공통 응답 형식(`success/data/error`)과 에러코드 표 그대로 구현
- `@RestControllerAdvice` + 커스텀 예외 클래스
- 다른 API보다 먼저 만들어두면 이후 컨트롤러들이 이걸 바로 재사용 가능
- 커밋: `feat: 공통 응답 포맷 및 예외 처리 추가`

### Step 5 — 분석 요청 생성 API (`feat/analysis-request`) ✅ 대부분 완료 (2026-08-13 재확인)
- `POST /api/analyses` 이미 구현됨 (`AnalysisController`→`AnalysisService.create`). Request DTO 검증, `regionSggCode` 서울 25개 자치구 검증(`SeoulDistrict.findByCode`, 실패 시 `REGION_NOT_SUPPORTED`) 전부 있음
- 업종 매칭도 `IndustryMatcher`로 이미 구현됨(LLM 대신 텍스트 포함/토큰겹침 기반 mock — 의도된 설계, 나중에 LLM으로 교체 가능한 구조) — **다만 위 미결 2번대로 96개 제한이 빠져 있어 이 부분만 손볼 것**
- 커밋: `feat: 분석 요청 생성 API 구현` (이미 커밋됐다면 미결 2번 수정은 별도 `fix:` 커밋으로)

### Step 6 — 3레이어 진단 스코어링 (`feat/diagnosis-scoring`) ← **가장 핵심** · ⚠️ 구현은 돼 있으나 배점이 v2 (위 미결 1번)

**시작 전에 `레이어_점수산출_설계서.md`를 반드시 통독할 것.** 아래는 요약이다. `DiagnosisScorer`·`RiskGrader`·`AnalysisService.getDiagnosis`가 이미 구현돼 있으므로 이 Step은 "새로 만들기"가 아니라 **위 미결 1번 배점 수정 + 아래 기대값으로 재검증**이 할 일의 전부다.

- **점수 방향 확정: 높을수록 안전**(리스크 낮음). 더 이상 미결 아님
- **계산 방식: 백분위 기반.** `지표 점수 = (서울 분포 백분위 / 100) × 지표 배점`. 임의 구간 없음
- **배점(2026-08-13 v3): MARKET 30 / CUSTOMER 40(전부 데이터) / COMPETITION 30 = 총 100점 만점.** L2에 더는 "정성 검증" 같은 영구 미채움 구간이 없다 — 진단 시점에 총점이 이미 최종값
- 등급 경계(P33/P67)는 설계서의 표를 그대로 쓸 것 (v3 갱신값: L2 위험<18.9/보통18.9~24.9/안전≥24.9, 총점 위험<48.0/보통48.0~57.9/안전≥57.9). 임의로 바꾸면 분포가 1/3씩 안 갈린다
- **성능**: 백분위 테이블은 `@PostConstruct`에서 1회 계산해 캐싱. 요청마다 재정렬하면 느리다
- **LOW_SAMPLE**: 점포수 n<10이면 밀도 점수를 중앙값으로 보정하고 `confidenceStatus=LOW_SAMPLE`
- **NULL 처리**: 업종 매핑 실패 시 market/customer는 NULL, `dataCoverage=COMPETITION_ONLY`. **100점 만점으로 환산하지 말 것**
- `layer_evidences`에 지표별로 개별 행 저장. **실제값("CAGR +4.0%")과 백분위("서울 상위 21%")를 둘 다** 남길 것
- **점수 계산 직후 `ai_summary` 생성** — 계산된 점수·근거를 컨텍스트로 OpenAI 호출, 종합진단 문구를 `diagnosis_results.ai_summary`에 저장(신규, 2026-08-12). LLM 실패 시 NULL로 두고 `verdict` 문구만 노출
- `GET /api/analyses/{id}/diagnosis`

**검증용 기대값** (구현 후 이 값이 나오는지 확인, 2026-08-13 v3 재산출):

| 케이스 | L1 | L2 | L3 | 총점/100 |
|---|---|---|---|---|
| 카페 / 중랑구 | 22.3 | 32.0 | 12.3 | 66.5 (안전) |
| 카페 / 강남구 | 22.3 | 32.0 | 2.4 | 56.6 (보통) |

- 커밋: `feat: 3개 레이어 리스크 진단 로직 및 조회 API 구현`

### Step 7 — 근거·가설 조회 API (`feat/evidence-hypothesis`) ✅ 완료 (2026-08-13 재확인)
- `GET /api/analyses/{id}/evidence` 이미 구현됨 (`AnalysisService.getEvidence`) — `confidenceStatus=CONFIRMED`인 근거와 미검증 가설을 분리해서 반환
- 커밋: `feat: 근거 및 미검증 가설 조회 API 구현` (이미 커밋됐을 가능성 높음, 확인만 할 것)

### Step 8 — 질문지 생성/조회 API (`feat/questionnaire`)
- `POST /api/analyses/{id}/questionnaires`, `GET .../{qid}` — LLM으로 유도질문 배제한 질문 생성
- 질문은 "과거 경험·현재 해결 방식·발생 빈도·실제 지출"을 묻는 형태로만. "이런 서비스 있으면 쓰시겠어요?" 같은 유도질문 금지
- **LLM 사용 경계**: 가설·질문 생성에만 쓴다. **L1/L2/L3의 숫자를 LLM으로 만들어내면 절대 안 됨** — 없는 데이터를 지어내는 것이라 서비스 정체성과 정면 충돌. 데이터 없으면 NULL로 두고 "확인 불가"로 표시
- **질문 생성과 함께 유도질문 자가검증 문구도 생성** — 방금 만든 질문이 왜 유도질문이 아닌지 1~2문장으로 설명, `questionnaires.leading_question_check_passed`/`leading_question_check_summary`에 저장(신규, 2026-08-12). 이것도 새 데이터를 만드는 게 아니라 생성된 질문 텍스트에 대한 자기설명이라 원칙 위반 아님
- **(2026-08-13) 응답 결과 반영·점수 갱신 API는 만들지 않는다.** 질문지는 이미 확정된 점수의 "이유"를 설명하는 용도이지 점수를 채우는 용도가 아니다. `projectedMax` 같은 점수 상승 예측 필드도 응답에 넣지 않는다.
- 커밋: `feat: 검증 질문지 생성 및 조회 API 구현`

### Step 9 — 결제·크레딧 API (`feat/payments`) — 신규 (2026-08-13)
- `POST /api/analyses/{id}/payments` — plan(SINGLE/PACK3) + paymentMethod 받아 목업 결제 처리(**실제 PG 연동 없음**), `analysis_requests.payment_status`를 PAID로 변경. PACK3면 `payment_credits`에 `credits_remaining=2`, `expires_at=6개월 후` 레코드 생성
- `GET /api/credits` — 세션의 잔여 크레딧·만료일 조회
- **Step 5(분석 요청 생성)에 훅 추가**: 새 진단 생성 시 세션에 유효 크레딧이 있으면 자동 차감 + 즉시 `payment_status=PAID`로 생성 (뒤로 미루면 안 됨 — Step 5 컨트롤러를 다시 손대야 하므로 이 스텝에서 같이 처리)
- **Step 6(진단 결과 조회) 응답에 게이팅 추가**: `accessLevel`(FREE/PAID) 필드, FREE면 `layers[].factors`/`layers[].summary`/`aiSummary`를 null로 내림
- **Step 8(질문지)·리포트 열람 가드**: `payment_status≠PAID`면 `PAYMENT_REQUIRED`(402)
- 익명 세션: 프론트가 `X-Session-Id` 헤더로 UUID 전달, 서버는 없으면 신규 발급해 응답 헤더로 내려줌
- 상세 스펙: 위 "⚠️ 미결 6건" 항목 5번, 노션 API 명세서 신규 섹션
- 커밋: `feat: 결제·크레딧 API 및 진단 결과 접근 등급 구현`

### Step 10 — (제외, 2026-08-12) ~~검증 결과 입력 API~~

**만들지 않기로 했다.** 서비스 범위가 "질문지 생성까지"로 확정되면서 이 단계는 제외됐다 — 상세 사유는 위 "⛔ 서비스 범위 재확정" 참고. `VerificationResult`/`CustomerScoreHistory` 엔티티는 이미 있어도 이 위의 API·컨트롤러는 만들지 않는다.

### Step 11 — 나머지 조회 API (`feat/misc-endpoints`)
- `GET /api/analyses` (목록), `GET /api/analyses/{id}/status` (상태) — `customer-history`는 Step 10 제외로 함께 제외
- 커밋: `feat: 분석 목록/상태 조회 API 구현`

### Step 12 — 통합 테스트 & 문서 마무리
- 주요 API 통합테스트 (H2, `application-test.yml` 이미 세팅됨)
- 커밋: `test: 분석 요청~진단 플로우 통합테스트 추가`
- README에 실행 방법 정리
- 커밋: `docs: README에 실행 방법 및 API 문서 링크 추가`

### Step 13 — 배포 준비
- `application-prod.yml` 점검 (`ddl-auto: validate` 이므로 반드시 로컬에서 스키마 확정 후 진행)
- 커밋: `chore: 배포 설정 점검`

---

## 우선순위 메모

시간이 부족하면 Step 5~7(분석 요청 생성 + 진단 결과 조회 + 근거 조회)까지가 데모의 핵심이다. Step 8(질문지 생성)까지 끝내면 **기능적으로는 서비스 전체 범위가 완성**이다. 다만 Figma에 결제 화면이 이미 있으므로 데모에서 그 플로우까지 보여주려면 **Step 9(결제·크레딧)도 필수**로 봐야 한다. Step 10은 제외(2026-08-12), Step 11은 목록/상태 조회만 남는다.

(참고: 투자자용 요약 리포트 확장은 지금 개발 범위에 포함하지 않음 — 노션 "무영" 페이지 확장 경로에 로드맵으로만 기록해둠)
