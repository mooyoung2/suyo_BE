# Suyo · 레이어 점수 산출 설계서 (v2 · 백분위 기반)

작성일 2026-08-11. **2026-08-13 v3 갱신**: L2 "정성 검증" 12점(구조적으로 절대 못 채우던 부분)을 없애고, 데이터로 확인되는 4개 지표의 배점을 재분배해 L2도 순수 데이터만으로 40점 만점에 도달하도록 재설계했다. 이제 총점은 진짜 100점 만점(30+40+30)이다. 질문지 기능은 "빈 점수를 채우는" 도구에서 "이미 나온 점수의 이유를 정성적으로 설명·검증하는" 도구로 역할이 바뀐다 (자세한 내용은 3번 섹션).

**v1에서 바뀐 것**: v1은 "10점/8점/6점/4점/2점" 같은 임의 배점과 "70%/40%" 같은 임의 등급컷을 썼다. 보기엔 깔끔하지만 그 숫자가 왜 그 숫자인지 설명할 근거가 없었다. v2는 **모든 점수와 등급 경계를 실제 데이터 분포에서 직접 도출**한다. 임의로 정한 숫자가 하나도 없다.

---

## 0. 핵심 원칙 — 백분위 = 점수

각 지표는 **서울 실제 분포에서 몇 등인지(백분위)를 그대로 점수로 환산**한다.

```
지표 점수 = (그 업종의 백분위 순위 / 100) × 해당 지표 배점
```

예시: 어떤 업종의 5년 CAGR이 서울 63개 업종 중 상위 20%(백분위 80)라면 → CAGR 배점 15점 × 0.80 = **12.0점**

이 방식의 장점:

- **구간 경계를 임의로 정할 필요가 없다.** "CAGR 3.4%가 왜 경계인가?"라는 질문 자체가 사라진다
- **구간 경계에서 점수가 뚝 끊기지 않는다.** CAGR 3.39%와 3.41%가 3점 차이 나는 불합리가 없어짐
- 데이터가 갱신되면 분포도 같이 갱신되므로 기준이 자동으로 최신화됨

**방향 처리**: 지표마다 "높을수록 좋은지"가 다르므로 아래와 같이 처리한다.

| 지표 | 방향 |
|---|---|
| 시장규모, CAGR, 모멘텀, 실수요증가율, 생존율 | 높을수록 좋음 → 백분위 그대로 |
| 성별 쏠림, 연령 쏠림, 경쟁밀도 | 낮을수록 좋음 → (100 − 백분위) |
| 주말 매출 비중 | **중앙값에서 멀수록 나쁨** → 중앙값과의 거리를 구한 뒤 (100 − 백분위) |

---

## 1. 레이어 구성과 배점

| 레이어 | 배점 | 지표 | 지표 배점 |
|---|---|---|---|
| **L1 시장** | 30 | 시장 규모 (최근 4분기 매출 합) | 10 |
| | | 5년 CAGR | 15 |
| | | 최근 모멘텀 (최근1년 − CAGR) | 5 |
| **L2 고객** | 40 | 성별 분산 | 7 |
| | | 연령 분산 | 7 |
| | | 실수요 증가율 (매출건수 CAGR) | 11 |
| | | 소비패턴 안정성 (주말비중) | 15 |
| **L3 경쟁** | 30 | **실질 경쟁 강도 (점포당 유동인구)** | 15 |
| | | 업종 5년 생존율 | 15 |

**(2026-08-13 v3) L2도 100% 데이터로 40점 전부 도달 가능하다.** v2에서는 성별·연령·실수요·소비패턴 4개 지표가 28점만 채우고 나머지 12점("정성 검증")은 질문지 응답을 실제로 입력받아야만 채워지는 구조였는데, 검증 결과 재입력 기능 자체를 서비스 범위에서 뺐기 때문에(2026-08-12 확정) 이 12점은 영구히 0으로 고정되는 죽은 배점이었다. v3는 이 문제를 없애기 위해 기존 4개 지표의 배점을 28→40으로 비례 확대(×10/7)했다 — 순위(백분위)는 그대로 두고 배점만 키운 것이라 새로운 데이터나 재계산이 필요 없다. 그 결과 L1(30)+L2(40)+L3(30) = **총 100점 만점**이 실제로 도달 가능해졌다. 질문지의 역할은 3번 섹션 참고.

**레이어 가중치 30/40/30 근거**: CB Insights 실패원인 1위가 PMF 실패(43%)이고, 이게 L2에 해당하므로 최대 배점.

---

## 2. 등급 경계 — 실제 총점 분포에서 도출

**계산 범위**: 매핑 가능한 업종 × 25개 자치구 = **2,395개 조합** 전부에 대해 점수를 실제로 계산했다.

### 2-1. 실제 점수 분포

| 백분위 | L1 (30) | L2 (40) | L3 (30) | 총점 (100) |
|---|---|---|---|---|
| P5 | 5.8 | 9.3 | 3.8 | 28.1 |
| P10 | 6.2 | 11.5 | 5.3 | 34.4 |
| P25 | 12.9 | 17.1 | 8.6 | 44.7 |
| **P33** | **15.6** | **18.9** | **9.2** | **48.0** |
| P50 | 17.7 | 22.3 | 11.8 | 52.9 |
| **P67** | **21.6** | **24.9** | **14.5** | **57.9** |
| P75 | 23.0 | 26.9 | 16.0 | 60.6 |
| P90 | 24.9 | 31.2 | 19.7 | 65.4 |
| P95 | 26.3 | 31.2 | 21.8 | 68.9 |

(2026-08-13 v3로 재계산. `db-init/data/scoring_reference_v3.py`로 2,395개 조합 전부 재산출 — 백분위 순위 자체는 안 바뀌었고 L2 배점만 28→40으로 늘어서 L2·총점 열만 갱신됐다.)

### 2-2. 등급 컷 = 3분위(P33 / P67)

등급을 3개로 나누므로, 실제 분포를 3등분하는 지점(P33, P67)을 그대로 경계로 쓴다.

| 레이어 | 위험 (HIGH) | 보통 (MEDIUM) | 안전 (LOW) |
|---|---|---|---|
| L1 시장 (30점) | < 15.6 | 15.6 ~ 21.6 | ≥ 21.6 |
| L2 고객 (40점) | < 18.9 | 18.9 ~ 24.9 | ≥ 24.9 |
| L3 경쟁 (30점) | < 9.2 | 9.2 ~ 14.5 | ≥ 14.5 |
| **총점 (100점 기준)** | **< 48.0** | **48.0 ~ 57.9** | **≥ 57.9** |

### 2-3. 등급 분포 검증

경계를 적용해서 2,395개 조합을 실제로 분류해봤다.

| 레이어 | 안전 | 보통 | 위험 |
|---|---|---|---|
| L1 | 34.3% | 33.4% | 32.4% |
| L2 | 35.3% | 33.4% | 31.3% |
| L3 | 33.0% | 38.5% | 28.5% |
| **총점** | **33.0%** | **33.9%** | **33.0%** |

거의 정확히 1/3씩 갈린다. **"안전"이 남발되지도, "위험"만 잔뜩 나오지도 않는다는 뜻.** 등급이 변별력을 갖는다.

> **중요**: 이 등급은 **"서울 안에서의 상대적 위치"**를 뜻한다. "안전 = 절대적으로 안전한 사업"이 아니라 "서울의 다른 업종·지역 조합과 비교했을 때 상위 1/3"이라는 의미다. 화면에도 이렇게 표기해야 오해가 없다.

---

## 3. 질문지의 역할 (2026-08-13 v3로 재정의)

**v3부터는 L2도 100% 데이터로 40점 전부 나온다.** 그래서 이전 버전에 있던 "정성 검증 12점 / 응답 건수별 점수표 / 검증 전·후 점수 프로젝션(예: 59→71)" 개념은 전부 폐기한다. `customer_score`를 나중에 올려주는 메커니즘 자체가 없어졌다 — 총점은 진단 시점에 이미 최종값이다. `verification_results` 테이블·`POST verification-results` 계획, "응답 N건 반영 시 예상 점수" 문구는 화면·API 어디에도 남기지 않는다.

**그럼 질문지는 왜 필요한가 — 점수가 아니라 '이유'를 채운다.** 백분위 점수는 "얼마나 좋은 조건인지"는 말해주지만 "왜 그런지"는 말해주지 않는다. 예: 실수요 증가율이 상위 22%로 높게 나와도, 그게 접근성 때문인지 가격 때문인지는 결제 데이터만으로 알 수 없다. 질문지는 이미 확정된 점수에 **정성적 설명·확신을 더하는 도구**로 역할이 바뀐다 — 응답을 안 받아도 점수·등급·판정은 이미 완전하다는 뜻이고, 이게 오히려 "질문지 없이도 서비스가 완결된다"는 기존 설계 원칙(6번 섹션 "우리가 안 하는 것")과 더 잘 맞는다.

화면 문구도 이에 맞춰 바꿔야 한다: "미충족 니즈 10점은 설문으로만 메울 수 있습니다" 같은 **점수-채움형 문구는 전부 삭제**하고, "왜 그런지는 데이터에 안 남습니다 — 질문지로 확인해 보세요" 같은 **설명-보강형 문구**로 교체한다.

---

## 4. 실제 계산 결과 검증 (v3 재산출, 2026-08-13)

| 케이스 | L1 | L2 | L3 | 총점/100 |
|---|---|---|---|---|
| 카페 / 중랑구 (540개) | 22.3 안전 | 32.0 안전 | 12.3 보통 | **66.5 안전** |
| 카페 / 종로구 (1,248개) | 22.3 안전 | 32.0 안전 | 4.4 위험 | **58.6 안전** |
| 카페 / 강남구 (2,123개) | 22.3 안전 | 32.0 안전 | 2.4 위험 | **56.6 보통** |
| 편의점 / 강남구 (803개) | 17.7 보통 | 27.8 안전 | 7.6 위험 | **53.1 보통** |

**전체 최상위**: 피부/비뇨기과 의원 / 도봉구 (80.9) — 고성장 + 보건의료 생존율(최고) + 경쟁 희소
**전체 최하위**: 기숙사·고시원 / 강남구 (10.2) — 매출 마이너스 성장 + 실수요 급감

같은 카페라도 중랑구 66.5(안전) vs 강남구 56.6(보통)로 갈린다. 지역 선택의 의미가 점수에 제대로 반영됨. (총점 등급은 총점 자체의 분포(48.0/57.9)로 매기므로, L3가 "위험"이어도 총점은 "안전"이 나올 수 있다 — 종로구 사례 참고.)

---

## 5. 표본 크기 처리 (L3)

6,004개 조합 중 **1,392개(23.2%)가 점포수 10개 미만**, **2,825개(47.1%)가 30개 미만**이다. 분모가 작으면 배수가 크게 튄다(3개→6개면 2배).

| 점포수 n | 처리 |
|---|---|
| n < 10 | `confidenceStatus = LOW_SAMPLE`. 밀도 점수는 백분위 대신 **중앙값(7.5점)** 부여, 화면에 "표본이 적어 참고용" 명시 |
| 10 ≤ n < 30 | 정상 산출하되 근거 문구에 실제 점포수 병기 |
| n ≥ 30 | 정상 처리 |

---

## 6. NULL 처리

전체 247개 소분류 중 **96개만 서울 매출데이터와 매핑**돼 있다. 나머지 약 61%는 L1·L2를 계산할 수 없다.

| 상황 | market | customer | competition | data_coverage |
|---|---|---|---|---|
| 매핑 성공 | 0~30 | 0~40 | 0~30 | FULL |
| 매핑 실패 | **NULL** | **NULL** | 0~30 | COMPETITION_ONLY |

**매핑 실패 시 100점 만점으로 환산하지 않는다.** 30점 만점에 24점을 "80점"으로 바꿔 보여주면 시장·고객을 전혀 모르는 상태인데 안전한 것처럼 오해하게 된다.

### 6-1. UX 결정: 업종 검색을 96개로 제한 (2026-08-12)

위 표의 "매핑 실패" 케이스가 전체 입력의 61%(151/247개 소분류)에서 발생한다는 게 문제였다. 다만 전국사업체수로 가중하면 매핑된 96개가 **71.7%**를 커버하고, 특히 예비창업자가 실제로 찾는 업종(외식 76.7%·부동산 100%·소매 53.1%)은 대부분 96개 안에 있다. 안 잡히는 151개는 컨설팅·법무·설계 등 B2B 위주이고, 서울시 상권분석서비스 원천 데이터 자체가 63개 업종으로만 분류돼 있어 매핑을 더 늘릴 방법이 없다(구조적 상한).

그래서 **입력 단계에서 업종 검색·선택을 매핑된 96개로 제한**하기로 했다. 사용자가 애초에 151개를 선택할 수 없으므로:

- `data_coverage = COMPETITION_ONLY`(L1·L2 NULL) 케이스는 정상 플로우에서 나오지 않는다
- 위 NULL 처리 로직은 **방어 로직으로 유지**한다 (API를 직접 호출하거나 데이터가 나중에 바뀌는 경우 대비)
- 화면 설계: 노션 API 명세서 "0. 지원 업종 조회" 엔드포인트, 노션 "무영" 페이지 디자인 스펙 참고

---

## 7. 인구 데이터 확보 완료 (population_by_sgg)

**유동인구·상주인구·직장인구 3종을 확보해서 자치구 단위로 집계 완료했다.**

- 출처: 서울시 상권분석서비스 (길단위인구·상주인구·직장인구, **행정동** 단위)
- 처리: 행정동 425개 → `adstrd_cd` 앞 5자리로 자치구 25개에 집계
- 결과: `population_by_sgg.csv` **525행** (25개구 × 21분기, 2021 1Q~2026 1Q — 매출 데이터와 기간 완전 일치)
- **검증 통과**: 2026년 1분기 상주인구 합계 **9,360,421명** — 서울시 실제 인구(약 930만)와 일치

| 구분 | 상위 자치구 | 확인된 사실 |
|---|---|---|
| 상주인구 | 송파(65만) 강서(56만) 강남(54만) | 실제 인구 순위와 일치 |
| 직장인구 | 강남(108만) 서초(43만) 중구(41만) 영등포(40만) | 오피스 상권이 정확히 잡힘 |
| 유동인구 | 강남(1.45억) 관악(1.27억) 송파(1.19억) | 분기 누적치 (일평균은 ÷90) |

### 이 데이터로 L3 경쟁 지표가 개선됐다

기존 "자치구 점포수 ÷ 서울평균"은 **그 지역에 사람이 얼마나 다니는지를 무시**했다. 점포가 많아도 유동인구가 그만큼 많으면 실제 경쟁은 덜하다. 그래서 **점포당 유동인구**로 교체한다.

실제로 순위가 이렇게 바뀐다 (카페 기준):

| 자치구 | 점포수 | 단순 밀도 순위 | 점포당 유동인구 순위 | 변화 |
|---|---|---|---|---|
| 강남구 | 2,123 | 25위 | **20위** | 개선 — 점포가 많지만 유동인구도 압도적 |
| 중구 | 1,133 | 19위 | **25위** | 악화 — 점포 대비 유동인구가 가장 적음 |
| 종로구 | 1,248 | 21위 | **24위** | 악화 |
| 중랑구 | 540 | 3위 | **2위** | 개선 |
| 도봉구 | 389 | 1위 | **4위** | 악화 — 점포는 적지만 유동인구도 적음 |

"점포가 적으니 좋다"가 아니라 "손님 대비 경쟁자가 적으니 좋다"로 바뀐 것. 훨씬 정확한 경쟁 지표다.

### 알아야 할 한계 두 가지

1. **자치구별 침투율(매출÷유동인구)은 계산할 수 없다.** 우리 매출 데이터(`seoul_sales_quarterly`)는 **서울시 전체 × 업종** 단위이지 자치구별로 쪼개져 있지 않다. 따라서 "중랑구 카페 매출 ÷ 중랑구 유동인구"는 구할 수 없다. 인구 데이터는 L3 경쟁 지표와 지역 규모 참고용으로만 쓴다.
2. **상주인구는 연 1회만 갱신된다.** 4분기 값이 다음 해 1~3분기에 그대로 반복되므로, 시계열 성장률을 뽑으면 안 된다. 규모 지표로만 쓸 것.

### 아직 못 구한 것 (L3, 우선순위 낮음)

| 데이터 | 소재 | 상태 |
|---|---|---|
| 소규모상가 임대료 | 공공데이터포털 `data.go.kr/data/15069766` (한국부동산원) | CSV 정식 제공 확인. "지역별"이 자치구 단위인지는 파일을 열어봐야 함 |
| 개·폐업 추이 | — | 쓸 만한 공개 데이터 못 찾음 |

둘 다 없어도 3개 레이어 점수가 전부 산출되므로 개발을 막지 않는다.

---

## 8. 구현 시 주의사항

1. **백분위 테이블을 앱 기동 시 1회 계산해서 메모리에 캐싱할 것.** 요청마다 63개 업종·6,004개 조합을 다시 정렬하면 느리다. 데이터가 정적이므로 `@PostConstruct`에서 한 번만 만들면 된다.
2. **백분위 계산은 `bisect`(이진 탐색) 방식으로.** 정렬된 배열에 값의 삽입 위치를 찾아 순위를 구한다.
3. **모든 지표를 `layer_evidences`에 개별 행으로 저장.** 점수만 저장하면 "왜 이 점수인지" 화면에 못 보여준다. factor·value(실제값)·source·confidence_status를 함께 남길 것. 특히 **원본 실제값(예: "CAGR +4.0%")과 백분위(예: "서울 상위 23%")를 둘 다** 보여주면 설득력이 훨씬 높다.
4. **비율 지표엔 분모를 항상 같이 저장.** L3 경쟁 밀도는 점포수 n을 함께 기록해야 LOW_SAMPLE 판정이 가능하다.
5. **등급은 저장하지 말고 조회 시 계산.** 경계값이 바뀌면 과거 데이터도 자동으로 새 기준을 따른다.
6. **객단가는 점수화하지 말고 사실 정보로만 기록.** v1에서 객단가를 점수화했더니 카페가 8,177원으로 최하점을 받았다 — 업종 고유 특성을 리스크로 오인한 것이라 폐기했다. 대신 "건당 평균 8,177원"을 근거로만 표시해 사용자가 필요 거래량을 가늠하게 한다.

---

## 9. AI 생성 콘텐츠 확장 — 종합진단 요약 (2026-08-12 신규)

기존 원칙(Step 8 참고)은 "LLM은 가설·질문 생성에만 쓰고, L1/L2/L3 숫자는 LLM으로 만들지 않는다"였다. 여기에 **레이어별 AI 진단이 아니라 종합(총점) 단계에서 왜 이 점수가 나왔는지 설명하는 문구 1개**를 추가하기로 했다. 레이어 카드마다 AI 코멘트를 넣는 대신 총점 카드 아래 단 하나로 모으는 게 화면이 덜 산만하다는 판단.

이 기능은 원칙을 깨지 않는다 — **LLM이 새로운 숫자·근거를 만들어내는 게 아니라, 이미 `layer_evidences`에 저장된 실제값·백분위·verdict를 컨텍스트로 넣어 자연어로 종합 설명만 생성**하기 때문이다. 계산은 여전히 결정론적 로직(백분위 산출)이 전담하고, LLM은 그 결과를 문장으로 엮는 역할만 한다.

- 저장 위치: `diagnosis_results.ai_summary` (TEXT, nullable)
- 생성 시점: 3레이어 점수 계산 직후, 같은 트랜잭션 또는 후속 호출로 1회 생성 후 캐싱. 검증 결과 반영으로 점수가 갱신되면 재생성
- 입력 컨텍스트: `totalScore`, `verdict`, 레이어별 `score`/`riskLevel`/`summary`, `layer_evidences`의 factor·value·percentile 전부
- 실패 시: `ai_summary`는 NULL로 두고 화면은 기존 규칙 기반 `verdict` 문구만 노출 (LLM 실패가 진단 결과 자체를 막으면 안 됨)
- API·화면 스펙: 노션 "API 명세서" 4번 섹션, 노션 "무영" 페이지 디자인 스펙 참고

## 10. AI 생성 콘텐츠 확장 — 질문지 유도질문 자가검증 (2026-08-12 신규)

Step 8 질문지 생성 시 LLM은 이미 "과거 경험·현재 해결 방식·발생 빈도·실제 지출"만 묻도록 프롬프트가 제약돼 있다. 여기에 **LLM이 질문을 만든 직후, 방금 만든 질문이 왜 유도질문이 아닌지를 스스로 1~2문장으로 설명**하도록 같은 호출(또는 후속 호출)에서 함께 생성하게 한다. 화면의 "유도질문 검사 통과" 카드는 지금 정적 문구인데, 이걸 실제 생성된 질문 내용을 근거로 든 동적 문구로 바꾸는 것.

- 저장 위치: `questionnaires.leading_question_check_passed` (BOOLEAN), `questionnaires.leading_question_check_summary` (TEXT)
- 이것도 숫자·근거 데이터를 만들어내는 게 아니라 **이미 생성된 질문 텍스트를 스스로 설명**하는 것이라 원칙 위반이 아니다
- `passed=false`가 나오면(자가검증 실패) 재생성 1회 시도 후에도 실패하면 `passed=false`로 그대로 노출하고 사용자가 "질문 직접 추가"로 보완하게 한다 — 실패를 숨기지 않는다
- API·화면 스펙: 노션 "API 명세서" 6번 섹션, 노션 "무영" 페이지 디자인 스펙 참고

## 11. 유료화 게이팅이 이 문서에 미치는 영향 (2026-08-13 신규)

건별 결제 모델(1건 9,900원 / 3건팩 24,900원, 구독 없음)이 확정되면서 `GET /api/analyses/{id}/diagnosis` 응답에 `accessLevel`(FREE/PAID)이 추가된다. **단, 이 문서의 계산 로직 자체는 전혀 바뀌지 않는다** — 무료든 유료든 서버는 항상 3레이어 전체를 계산한다. 달라지는 건 응답에서 무엇을 내려주느냐뿐이다.

| 필드 | FREE | PAID |
|---|---|---|
| `totalScore`, `verdict`, `dataCoverage` | 노출 | 노출 |
| `layers[].score`, `riskLevel` | 노출 (총점 구성 요소라 항상 보여줌) | 노출 |
| `layers[].factors`, `layers[].summary` | `null` | 노출 |
| `aiSummary` | `null` | 노출 |

즉 **점수는 무료에서도 항상 계산·저장하고, 결제 여부는 "보여주느냐"만 결정**한다. 점수 계산을 결제 여부에 따라 다르게 하면(예: 결제 전엔 대충 계산) 정확도를 신뢰할 수 없게 되므로 절대 안 된다. 상세 스펙은 `개발_킥오프_가이드.md` 미결 5번, 노션 API 명세서 참고.

## 12. "유사기업 12곳 추론" 기능 — 범위 제외 (2026-08-13)

피그마 목업 일부 화면(진단 결과·리포트)에 "유사 조건 12곳 중 3년 생존 7곳, 생존군은 개업 6개월 내 객단가를 평균 18% 올렸다" 같은 **개별 점포 단위 추론 콘텐츠**가 들어가 있었다. 이건 만들 수 없다 — 저희가 가진 공공데이터 6종(업종 마스터, 서울 분기 매출, 자치구별 점포수, 자치구별 인구, 업종별 생존율, 업종코드 연결표)은 전부 **업종×지역 단위 집계치**이고, 개별 점포가 언제 생겼다 언제 망했는지, 그 점포가 가격을 얼마나 올렸는지 같은 **점포 단위 시계열**은 어디에도 없다. 이런 데이터를 만들려면 별도의 점포 단위 추적 데이터셋이 필요한데 현재 범위 밖이다.

**결정(2026-08-13): 이 기능은 완전히 뺀다.** 화면에서도, API에서도, `layer_evidences`/`ai_summary`에서도 "유사기업 N곳" 형태의 문구·근거는 생성하지 않는다. 화면 쪽 정리는 별도로 반영 중(피그마 06·08·09·11의 "유사 조건 12곳" 블록, "추론(≈)" 배지 제거 — 데이터 출처 배지는 다시 **실측/검증 필요 2종만** 쓴다).

## 13. 총점 표기 — 100점 만점으로 확정 (2026-08-13)

11번 섹션 작성 시점엔 "정성 검증 12점이 구조적으로 안 채워지므로 총점은 88점 만점"이라고 정리했었다. 이건 **폐기**한다. 1번 섹션에서 L2 배점을 40점 전부 데이터로 재배분했으므로, 총점은 이제 정말로 L1(30)+L2(40)+L3(30) = **100점 만점**이고 화면의 "X / 100" 표기가 그대로 맞다. `GET /api/analyses/{id}/diagnosis` 응답의 `totalScore`도 항상 0~100 범위로 계산된다 (기존 문서에 있던 `maxScore: 88`류 언급은 전부 무효).
